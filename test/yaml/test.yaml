# This file is only meant for testing.
apiVersion: v1
kind: Pod
metadata:
  name: ${NAME}-pod # ${NAME} is a placeholder
providers:
  - Provider 1
  - Provider 2
  - Provider 3
spec:
  containers:
  - name: nginx
    image: ${IMAGE_TAG} # ${IMAGE_TAG} is a placeholder